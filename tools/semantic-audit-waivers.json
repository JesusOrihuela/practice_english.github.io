{ "_comment": "Persistent, human-reviewed waivers for semantic-audit.mjs. Key by item key (or a|b for dup); optional hash (content sha1[:12]) so a changed item re-flags; optional kinds[] to waive only some signals.", "items": {} }
